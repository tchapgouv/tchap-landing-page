server {
    server_name _;
    listen <%= ENV['PORT'] %>;

    index index.html;
    root /app/dist;
    location / {
        rewrite ^/faq.*$ https://aide.tchap.beta.gouv.fr/fr redirect;
        try_files $uri /index.html;
    }
}
